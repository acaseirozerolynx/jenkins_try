pipeline {
    agent any
    stages {
        stage('Setting the variables values') {
         steps {
            ls
         }
      }
        stage('Test') {
            steps {
                echo 'Test'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy'
            }
        }
    }
}
